meta {
  name: Update Actor
  type: http
  version: 1.0.0
}

put {{baseUrl}}/api/actors/:id {
  body: json({
    name: "Updated Actor",
    description: "Updated description"
  })

  response {
    status: 200
  }
}